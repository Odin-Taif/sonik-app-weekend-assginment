export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

type Post = {
  id: string;
  post: string;
  // content: string;
  // timestamp: string;
  // author: string;
};
