export type User = {
  id: string;
  name: string;
  email: string;
  hashedPassword: string;
};

type Post = {
  id: string;
  post: string;
  // content: string;
  // timestamp: string;
  // author: string;
};
